dir: "{{.InterfaceDir}}/mocks"
filename: "mock_{{.InterfaceName}}.go"
structname: "{{.InterfaceName}}"
pkgname: "mocks"
template: testify

packages:
  # Add entries here after creating interfaces that tests will mock.
  # Example:
  # github.com/org/project/svc/internal/services/example:
  #   interfaces:
  #     Service:
