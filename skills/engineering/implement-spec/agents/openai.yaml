interface:
  display_name: "Implement Spec"
  short_description: "Implement a specification through checks to a PR"
  default_prompt: "Use $implement-spec to implement the supplied specification, coordinate independent verification, and create a pull request."
policy:
  allow_implicit_invocation: false
